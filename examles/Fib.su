define fib n
    || computes the `n`-th fibonacci number
    where
        fib : Integer -> Integer
    as n.match
        0 -> 0
        1 -> 1
        _ -> (fib (n - 1)) + (fib (n-2))


define add_twice x y
    where
        add_twice : T -> T -> T
        T is Numeric
    as x + y + y
